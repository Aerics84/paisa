{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import os, json; p='graphify-out/graph.json'; print(json.dumps({'systemMessage': 'graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md before searching raw files.'})) if os.path.exists(p) else None\""
          }
        ]
      }
    ]
  }
}
