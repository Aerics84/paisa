model = "gpt-5.4"
approval_policy = "on-request"
approvals_reviewer = "auto_review"
sandbox_mode = "workspace-write"
web_search = "cached"
plan_mode_reasoning_effort = "medium"

[mcp_servers.codebase-memory-mcp]
enabled = false

# Repo intent:
# - Follow AGENTS.md before patching.
# - Prefer codebase-memory-mcp for architecture/ownership discovery.
# - Use local skills for architecture scan, Memory ownership and Screeps guards.
